extends ../layout

block content
  .login_container
    .page-header
      h3 Welcome to bidin.bio!
      br
      h3 Before logging in, make sure your business account on Instagram is linked to a Facebook page
      hr

    form.form-horizontal(method='POST')
      .form-group.row.justify-content-md-center
        a.btn.btn-block.btn-facebook.btn-social(href='/auth/facebook')
          i.fa.fa-facebook
          | Sign in with Facebook
