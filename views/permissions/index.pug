extends ../layout

block content
  .login_container
    .page-header
      h3 Welcome to bidin.bio!
      br
      h3 Unfortunately, we found that you don't give all the necessary permissions. Please click to Facebook button and check that all checkbox is set. 
      hr

    form.form-horizontal(method='POST')
      .form-group.row.justify-content-md-center
        a.btn.btn-block.btn-facebook.btn-social(href='/auth/facebook')
          i.fa.fa-facebook
          | Sign in with Facebook
